# ch03-virtual-filesystem

**章节标题**：虚拟文件系统 — Deep Agents 的 Context Engineering 核心

本页文件由教程 HTML 直接抽取。文件扩展名根据内容启发式确定 (.py / .sh / .toml / .json / .yaml / .md / .txt)。Python 文件已通过 `ast.parse` 语法检查；其余类型仅保证结构与原文一致。

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 内置文件系统工具 / read_file ：不只是”读文件”

- 片段 1/1 (illustrative): [`内置文件系统工具__read_file-不只是”读文件”.txt`](内置文件系统工具__read_file-不只是”读文件”.txt)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 内置文件系统工具 / grep ：三种输出模式

- 片段 1/1 (python): [`内置文件系统工具__grep-三种输出模式.py`](内置文件系统工具__grep-三种输出模式.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 上下文自动管理：不只是存文件 / 大结果自动卸载

- 片段 1/1 (illustrative): [`上下文自动管理-不只是存文件__大结果自动卸载.txt`](上下文自动管理-不只是存文件__大结果自动卸载.txt)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 可插拔的存储后端 / StateBackend（默认）：临时存储

- 片段 1/1 (python): [`可插拔的存储后端__StateBackend默认-临时存储.py`](可插拔的存储后端__StateBackend默认-临时存储.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 可插拔的存储后端 / FilesystemBackend：本地磁盘

- 片段 1/1 (python): [`可插拔的存储后端__FilesystemBackend-本地磁盘.py`](可插拔的存储后端__FilesystemBackend-本地磁盘.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 可插拔的存储后端 / LocalShellBackend：本地 Shell 执行

- 片段 1/1 (python): [`可插拔的存储后端__LocalShellBackend-本地-Shell-执行.py`](可插拔的存储后端__LocalShellBackend-本地-Shell-执行.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 可插拔的存储后端 / StoreBackend：跨会话持久化

- 片段 1/2 (python): [`可插拔的存储后端__StoreBackend-跨会话持久化__01.py`](可插拔的存储后端__StoreBackend-跨会话持久化__01.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 可插拔的存储后端 / StoreBackend：跨会话持久化

- 片段 2/2 (python): [`可插拔的存储后端__StoreBackend-跨会话持久化__02.py`](可插拔的存储后端__StoreBackend-跨会话持久化__02.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 可插拔的存储后端 / CompositeBackend：混合路由

- 片段 1/1 (python): [`可插拔的存储后端__CompositeBackend-混合路由.py`](可插拔的存储后端__CompositeBackend-混合路由.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 可插拔的存储后端 / 沙箱后端：安全代码执行

- 片段 1/1 (shell): [`可插拔的存储后端__沙箱后端-安全代码执行.sh`](可插拔的存储后端__沙箱后端-安全代码执行.sh)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 自定义后端与安全策略 / 声明式权限：FilesystemPermission

- 片段 1/1 (python): [`自定义后端与安全策略__声明式权限-FilesystemPermission.py`](自定义后端与安全策略__声明式权限-FilesystemPermission.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 自定义后端与安全策略 / 实现自定义后端

- 片段 1/1 (illustrative): [`自定义后端与安全策略__实现自定义后端.txt`](自定义后端与安全策略__实现自定义后端.txt)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 自定义后端与安全策略 / 安全策略：PolicyWrapper

- 片段 1/2 (python): [`自定义后端与安全策略__安全策略-PolicyWrapper__01.py`](自定义后端与安全策略__安全策略-PolicyWrapper__01.py)

## 虚拟文件系统 — Deep Agents 的 Context Engineering 核心 / 自定义后端与安全策略 / 安全策略：PolicyWrapper

- 片段 2/2 (python): [`自定义后端与安全策略__安全策略-PolicyWrapper__02.py`](自定义后端与安全策略__安全策略-PolicyWrapper__02.py)

