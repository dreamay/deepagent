# ch06-async-subagents

**章节标题**：异步子 Agent — 让主 Agent 同时驱动多个子任务

本页文件由教程 HTML 直接抽取。文件扩展名根据内容启发式确定 (.py / .sh / .toml / .json / .yaml / .md / .txt)。Python 文件已通过 `ast.parse` 语法检查；其余类型仅保证结构与原文一致。

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 同步子 Agent 的瓶颈

- 片段 1/1 (illustrative): [`同步子-Agent-的瓶颈.txt`](同步子-Agent-的瓶颈.txt)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 配置异步子 Agent

- 片段 1/1 (python): [`配置异步子-Agent.py`](配置异步子-Agent.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 主 Agent 的 5 把”遥控器” / 一次完整生命周期

- 片段 1/1 (illustrative): [`主-Agent-的-5-把”遥控器”__一次完整生命周期.txt`](主-Agent-的-5-把”遥控器”__一次完整生命周期.txt)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 本地最小验证方案（单部署 + ASGI，可直接复制运行）

- 片段 1/1 (tree): [`两种传输-ASGI-vs-HTTP__本地最小验证方案单部署-+-ASGI，可直接复制运行.txt`](两种传输-ASGI-vs-HTTP__本地最小验证方案单部署-+-ASGI，可直接复制运行.txt)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 1 步：安装依赖

- 片段 1/2 (shell): [`两种传输-ASGI-vs-HTTP__第-1-步-安装依赖__01.sh`](两种传输-ASGI-vs-HTTP__第-1-步-安装依赖__01.sh)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 1 步：安装依赖

- 片段 2/2 (shell): [`两种传输-ASGI-vs-HTTP__第-1-步-安装依赖__02.sh`](两种传输-ASGI-vs-HTTP__第-1-步-安装依赖__02.sh)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 2 步：准备环境变量

- 片段 1/3 (python): [`两种传输-ASGI-vs-HTTP__第-2-步-准备环境变量__01.py`](两种传输-ASGI-vs-HTTP__第-2-步-准备环境变量__01.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 2 步：准备环境变量

- 片段 2/3 (python): [`两种传输-ASGI-vs-HTTP__第-2-步-准备环境变量__02.py`](两种传输-ASGI-vs-HTTP__第-2-步-准备环境变量__02.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 2 步：准备环境变量

- 片段 3/3 (python): [`两种传输-ASGI-vs-HTTP__第-2-步-准备环境变量__03.py`](两种传输-ASGI-vs-HTTP__第-2-步-准备环境变量__03.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 3 步：编写 langgraph.json

- 片段 1/1 (json): [`两种传输-ASGI-vs-HTTP__第-3-步-编写-langgraphjson.json`](两种传输-ASGI-vs-HTTP__第-3-步-编写-langgraphjson.json)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 4 步：编写一个故意运行很慢的 Subagent

- 片段 1/1 (python): [`两种传输-ASGI-vs-HTTP__第-4-步-编写一个故意运行很慢的-Subagent.py`](两种传输-ASGI-vs-HTTP__第-4-步-编写一个故意运行很慢的-Subagent.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 5 步：创建 Supervisor

- 片段 1/3 (python): [`两种传输-ASGI-vs-HTTP__第-5-步-创建-Supervisor__01.py`](两种传输-ASGI-vs-HTTP__第-5-步-创建-Supervisor__01.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 5 步：创建 Supervisor

- 片段 2/3 (python): [`两种传输-ASGI-vs-HTTP__第-5-步-创建-Supervisor__02.py`](两种传输-ASGI-vs-HTTP__第-5-步-创建-Supervisor__02.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 5 步：创建 Supervisor

- 片段 3/3 (python): [`两种传输-ASGI-vs-HTTP__第-5-步-创建-Supervisor__03.py`](两种传输-ASGI-vs-HTTP__第-5-步-创建-Supervisor__03.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 6 步：启动本地 Agent Server

- 片段 1/2 (shell): [`两种传输-ASGI-vs-HTTP__第-6-步-启动本地-Agent-Server__01.sh`](两种传输-ASGI-vs-HTTP__第-6-步-启动本地-Agent-Server__01.sh)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 6 步：启动本地 Agent Server

- 片段 2/2 (yaml): [`两种传输-ASGI-vs-HTTP__第-6-步-启动本地-Agent-Server__02.yaml`](两种传输-ASGI-vs-HTTP__第-6-步-启动本地-Agent-Server__02.yaml)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 7 步：使用 SDK 验证异步行为

- 片段 1/2 (typescript): [`两种传输-ASGI-vs-HTTP__第-7-步-使用-SDK-验证异步行为__01.ts`](两种传输-ASGI-vs-HTTP__第-7-步-使用-SDK-验证异步行为__01.ts)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / 第 7 步：使用 SDK 验证异步行为

- 片段 2/2 (text): [`两种传输-ASGI-vs-HTTP__第-7-步-使用-SDK-验证异步行为__02.txt`](两种传输-ASGI-vs-HTTP__第-7-步-使用-SDK-验证异步行为__02.txt)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 两种传输：ASGI vs HTTP / HTTP 传输（远程，按需切换）

- 片段 1/1 (python): [`两种传输-ASGI-vs-HTTP__HTTP-传输远程，按需切换.py`](两种传输-ASGI-vs-HTTP__HTTP-传输远程，按需切换.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 三种部署拓扑

- 片段 1/1 (python): [`三种部署拓扑.py`](三种部署拓扑.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 最佳实践 / 1. 本地开发要把 Worker Pool 调大

- 片段 1/1 (shell): [`最佳实践__1-本地开发要把-Worker-Pool-调大.sh`](最佳实践__1-本地开发要把-Worker-Pool-调大.sh)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 最佳实践 / 2. 描述要具体，行为导向

- 片段 1/1 (python): [`最佳实践__2-描述要具体，行为导向.py`](最佳实践__2-描述要具体，行为导向.py)

## 异步子 Agent — 让主 Agent 同时驱动多个子任务 / 常见问题排查 / 问题 1：刚启动就立刻轮询状态

- 片段 1/1 (python): [`常见问题排查__问题-1-刚启动就立刻轮询状态.py`](常见问题排查__问题-1-刚启动就立刻轮询状态.py)

