# ch10-sandboxes

**章节标题**：沙箱执行 — 让 Agent 安全地运行代码

本页文件由教程 HTML 直接抽取。文件扩展名根据内容启发式确定 (.py / .sh / .toml / .json / .yaml / .md / .txt)。Python 文件已通过 `ast.parse` 语法检查；其余类型仅保证结构与原文一致。

## 沙箱执行 — 让 Agent 安全地运行代码 / 1. 沙箱 Backend：执行环境，而不是权限开关 / execute() 的返回值

- 片段 1/1 (python): [`1-沙箱-Backend-执行环境，而不是权限开关__execute-的返回值.py`](1-沙箱-Backend-执行环境，而不是权限开关__execute-的返回值.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 3. 快速上手：LangSmithSandbox

- 片段 1/2 (shell): [`3-快速上手-LangSmithSandbox__01.sh`](3-快速上手-LangSmithSandbox__01.sh)

## 沙箱执行 — 让 Agent 安全地运行代码 / 3. 快速上手：LangSmithSandbox

- 片段 2/2 (python): [`3-快速上手-LangSmithSandbox__02.py`](3-快速上手-LangSmithSandbox__02.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 4. 当前 Python 沙箱集成

- 片段 1/1 (python): [`4-当前-Python-沙箱集成.py`](4-当前-Python-沙箱集成.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 5. 两种集成模式

- 片段 1/1 (shell): [`5-两种集成模式.sh`](5-两种集成模式.sh)

## 沙箱执行 — 让 Agent 安全地运行代码 / 6. 文件有两个平面 / 在运行前播种输入

- 片段 1/1 (python): [`6-文件有两个平面__在运行前播种输入.py`](6-文件有两个平面__在运行前播种输入.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 6. 文件有两个平面 / 在运行后提取产物

- 片段 1/1 (python): [`6-文件有两个平面__在运行后提取产物.py`](6-文件有两个平面__在运行后提取产物.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 7. 生命周期与作用域 / Thread-scoped：按对话复用

- 片段 1/1 (python): [`7-生命周期与作用域__Thread-scoped-按对话复用.py`](7-生命周期与作用域__Thread-scoped-按对话复用.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 7. 生命周期与作用域 / Assistant-scoped：跨对话复用

- 片段 1/1 (python): [`7-生命周期与作用域__Assistant-scoped-跨对话复用.py`](7-生命周期与作用域__Assistant-scoped-跨对话复用.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 8. Deep Agents Code：把工具调用指向远程沙箱 / 内置、第三方与配置提供商

- 片段 1/1 (text): [`8-Deep-Agents-Code-把工具调用指向远程沙箱__内置、第三方与配置提供商.txt`](8-Deep-Agents-Code-把工具调用指向远程沙箱__内置、第三方与配置提供商.txt)

## 沙箱执行 — 让 Agent 安全地运行代码 / 8. Deep Agents Code：把工具调用指向远程沙箱 / 关键 CLI 参数

- 片段 1/1 (illustrative): [`8-Deep-Agents-Code-把工具调用指向远程沙箱__关键-CLI-参数.txt`](8-Deep-Agents-Code-把工具调用指向远程沙箱__关键-CLI-参数.txt)

## 沙箱执行 — 让 Agent 安全地运行代码 / 9. LangSmith 托管沙箱：不止是一个 Backend / 独立 SDK 使用

- 片段 1/2 (python): [`9-LangSmith-托管沙箱-不止是一个-Backend__独立-SDK-使用__01.py`](9-LangSmith-托管沙箱-不止是一个-Backend__独立-SDK-使用__01.py)

## 沙箱执行 — 让 Agent 安全地运行代码 / 9. LangSmith 托管沙箱：不止是一个 Backend / 独立 SDK 使用

- 片段 2/2 (typescript): [`9-LangSmith-托管沙箱-不止是一个-Backend__独立-SDK-使用__02.ts`](9-LangSmith-托管沙箱-不止是一个-Backend__独立-SDK-使用__02.ts)

