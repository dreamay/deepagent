# ch11-filesystem-permissions

**章节标题**：文件系统权限 — 用声明式规则控制 Agent 的读写边界

本页文件由教程 HTML 直接抽取。文件扩展名根据内容启发式确定 (.py / .sh / .toml / .json / .yaml / .md / .txt)。Python 文件已通过 `ast.parse` 语法检查；其余类型仅保证结构与原文一致。

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 1. 权限控制的对象是什么

- 片段 1/1 (python): [`1-权限控制的对象是什么.py`](1-权限控制的对象是什么.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 2. 一条规则的三个字段

- 片段 1/1 (python): [`2-一条规则的三个字段.py`](2-一条规则的三个字段.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 3. 求值模型：首条匹配生效 / 正确的工作区白名单

- 片段 1/1 (python): [`3-求值模型-首条匹配生效__正确的工作区白名单.py`](3-求值模型-首条匹配生效__正确的工作区白名单.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 3. 求值模型：首条匹配生效 / 先保护敏感文件，再放行目录

- 片段 1/1 (python): [`3-求值模型-首条匹配生效__先保护敏感文件，再放行目录.py`](3-求值模型-首条匹配生效__先保护敏感文件，再放行目录.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 4. 四种常见策略 / 策略一：整个文件系统只读

- 片段 1/1 (python): [`4-四种常见策略__策略一-整个文件系统只读.py`](4-四种常见策略__策略一-整个文件系统只读.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 4. 四种常见策略 / 策略二：只能访问指定工作区

- 片段 1/1 (python): [`4-四种常见策略__策略二-只能访问指定工作区.py`](4-四种常见策略__策略二-只能访问指定工作区.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 4. 四种常见策略 / 策略三：共享知识只读，用户记忆可写

- 片段 1/1 (python): [`4-四种常见策略__策略三-共享知识只读，用户记忆可写.py`](4-四种常见策略__策略三-共享知识只读，用户记忆可写.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 4. 四种常见策略 / 策略四：拒绝所有文件访问

- 片段 1/1 (python): [`4-四种常见策略__策略四-拒绝所有文件访问.py`](4-四种常见策略__策略四-拒绝所有文件访问.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 5. interrupt ：把敏感写入交给人审批

- 片段 1/1 (python): [`5-interrupt-把敏感写入交给人审批.py`](5-interrupt-把敏感写入交给人审批.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 6. 子 Agent：默认继承，显式配置则整体替换

- 片段 1/1 (python): [`6-子-Agent-默认继承，显式配置则整体替换.py`](6-子-Agent-默认继承，显式配置则整体替换.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 7. CompositeBackend 与沙箱的特殊边界 / CompositeBackend：权限路径必须落在可控制的路由上

- 片段 1/1 (python): [`7-CompositeBackend-与沙箱的特殊边界__CompositeBackend-权限路径必须落在可控制的路由上.py`](7-CompositeBackend-与沙箱的特殊边界__CompositeBackend-权限路径必须落在可控制的路由上.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 8. 什么时候升级到自定义策略 / Policy Hook：在 Backend 层执行动态策略

- 片段 1/1 (illustrative): [`8-什么时候升级到自定义策略__Policy-Hook-在-Backend-层执行动态策略.txt`](8-什么时候升级到自定义策略__Policy-Hook-在-Backend-层执行动态策略.txt)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 8. 什么时候升级到自定义策略 / 两种实现方式

- 片段 1/1 (python): [`8-什么时候升级到自定义策略__两种实现方式.py`](8-什么时候升级到自定义策略__两种实现方式.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 8. 什么时候升级到自定义策略 / PolicyWrapper：可复用的策略包装器

- 片段 1/2 (python): [`8-什么时候升级到自定义策略__PolicyWrapper-可复用的策略包装器__01.py`](8-什么时候升级到自定义策略__PolicyWrapper-可复用的策略包装器__01.py)

## 文件系统权限 — 用声明式规则控制 Agent 的读写边界 / 8. 什么时候升级到自定义策略 / PolicyWrapper：可复用的策略包装器

- 片段 2/2 (text): [`8-什么时候升级到自定义策略__PolicyWrapper-可复用的策略包装器__02.txt`](8-什么时候升级到自定义策略__PolicyWrapper-可复用的策略包装器__02.txt)

